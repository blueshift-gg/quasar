[package]
name = "quasar-test-token-init"
version = "0.1.0"
publish = false
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
test = false
doctest = false

[lints]
workspace = true

[features]
default = ["alloc", "debug"]
alloc = []
debug = []
idl-build = ["quasar-lang/idl-build"]

[dependencies]
quasar-lang = { path = "../../../lang" }
quasar-spl = { path = "../../../spl" }
quasar-derive = { path = "../../../derive" }
