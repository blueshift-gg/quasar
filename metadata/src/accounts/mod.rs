//! Account behavior modules for Metaplex metadata wrappers.
//!
//! Each module exposes its `Args`, builder, and `AccountBehavior`
//! implementation for supported wrapper types.

pub mod master_edition;
pub mod metadata;
